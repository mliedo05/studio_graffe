<% content_for :head do %>
  <style>
    .floating-label-input:focus-within label,
    .floating-label-input input:not(:placeholder-shown) + label {
      transform: translateY(-1.5rem) scale(0.85);
      color: #7e7576;
    }
    .floating-label-input label {
      transition: all 0.2s ease-out;
      pointer-events: none;
    }
  </style>
<% end %>

<div class="min-h-screen flex flex-col items-center justify-center p-gutter bg-background">
  <!-- Background decorative -->
  <div class="fixed inset-0 -z-10 pointer-events-none">
    <div class="absolute top-0 right-0 w-1/2 h-full bg-gradient-to-l from-amber-50/20 to-transparent"></div>
    <div class="absolute bottom-0 left-0 w-1/3 h-1/2 from-amber-100/10 via-transparent to-transparent"></div>
  </div>

  <main class="w-full max-w-[500px] mx-auto">
    <div class="bg-surface-container-lowest rounded-xl shadow-sm border border-outline-variant/30 overflow-hidden px-10 py-12 flex flex-col gap-10">
      <!-- Logo -->
      <div class="text-center">
        <h1 class="text-4xl font-serif italic tracking-widest text-primary mb-2">Studio Graffé</h1>
        <p class="font-label-caps text-label-caps text-on-surface-variant tracking-[0.2em] uppercase">Crear cuenta</p>
      </div>

      <!-- Error messages -->
      <% if resource.errors.any? %>
        <div class="bg-red-50 border border-red-200 rounded-lg p-4 space-y-1">
          <p class="font-label-caps text-label-caps text-red-700 uppercase tracking-widest mb-2">Corrige los siguientes errores:</p>
          <% resource.errors.full_messages.each do |msg| %>
            <p class="font-body-sm text-body-sm text-red-600 flex items-start gap-2">
              <span class="material-symbols-outlined text-sm mt-0.5">error</span>
              <%= msg %>
            </p>
          <% end %>
        </div>
      <% end %>

      <!-- Form -->
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <div class="flex flex-col gap-8">
          <div class="space-y-6">
            <!-- Nombre y Apellido en fila -->
            <div class="grid grid-cols-2 gap-4">
              <div class="relative flex flex-col floating-label-input">
                <%= f.text_field :first_name, autocomplete: "given-name",
                      placeholder: " ",
                      class: "peer w-full bg-transparent border-0 border-b border-outline-variant py-2 focus:ring-0 focus:border-primary text-body-md transition-colors" %>
                <label class="absolute left-0 top-2 font-label-caps text-label-caps text-outline uppercase tracking-widest origin-left">
                  Nombre
                </label>
              </div>
              <div class="relative flex flex-col floating-label-input">
                <%= f.text_field :last_name, autocomplete: "family-name",
                      placeholder: " ",
                      class: "peer w-full bg-transparent border-0 border-b border-outline-variant py-2 focus:ring-0 focus:border-primary text-body-md transition-colors" %>
                <label class="absolute left-0 top-2 font-label-caps text-label-caps text-outline uppercase tracking-widest origin-left">
                  Apellido
                </label>
              </div>
            </div>

            <!-- Teléfono -->
            <div class="relative flex flex-col floating-label-input">
              <%= f.telephone_field :phone, autocomplete: "tel",
                    placeholder: " ",
                    class: "peer w-full bg-transparent border-0 border-b border-outline-variant py-2 focus:ring-0 focus:border-primary text-body-md transition-colors" %>
              <label class="absolute left-0 top-2 font-label-caps text-label-caps text-outline uppercase tracking-widest origin-left">
                Teléfono
              </label>
            </div>

            <!-- Email -->
            <div class="relative flex flex-col floating-label-input">
              <%= f.email_field :email, autofocus: true, autocomplete: "email",
                    placeholder: " ",
                    class: "peer w-full bg-transparent border-0 border-b border-outline-variant py-2 focus:ring-0 focus:border-primary text-body-md transition-colors" %>
              <label class="absolute left-0 top-2 font-label-caps text-label-caps text-outline uppercase tracking-widest origin-left">
                Email
              </label>
            </div>

            <!-- Password -->
            <div class="relative flex flex-col floating-label-input">
              <%= f.password_field :password, autocomplete: "new-password",
                    placeholder: " ",
                    class: "peer w-full bg-transparent border-0 border-b border-outline-variant py-2 focus:ring-0 focus:border-primary text-body-md transition-colors" %>
              <label class="absolute left-0 top-2 font-label-caps text-label-caps text-outline uppercase tracking-widest origin-left">
                Contraseña<% if @minimum_password_length %> (<%= @minimum_password_length %> mín.)<% end %>
              </label>
            </div>

            <!-- Password Confirmation -->
            <div class="relative flex flex-col floating-label-input">
              <%= f.password_field :password_confirmation, autocomplete: "new-password",
                    placeholder: " ",
                    class: "peer w-full bg-transparent border-0 border-b border-outline-variant py-2 focus:ring-0 focus:border-primary text-body-md transition-colors" %>
              <label class="absolute left-0 top-2 font-label-caps text-label-caps text-outline uppercase tracking-widest origin-left">
                Confirmar contraseña
              </label>
            </div>
          </div>

          <%= f.submit "Crear cuenta",
                class: "w-full bg-primary text-on-primary py-4 rounded-full font-label-caps text-label-caps uppercase tracking-widest transition-all duration-300 hover:scale-[1.02] hover:shadow-lg active:scale-95 cursor-pointer" %>
        </div>
      <% end %>

      <!-- Google OAuth -->
      <div class="flex flex-col gap-4">
        <div class="flex items-center gap-3">
          <div class="flex-1 h-px bg-outline-variant/30"></div>
          <span class="font-label-caps text-[10px] text-outline uppercase tracking-widest">o regístrate con</span>
          <div class="flex-1 h-px bg-outline-variant/30"></div>
        </div>
        <%= button_to user_google_oauth2_omniauth_authorize_path,
              method: :post,
              data: { turbo: false },
              class: "w-full flex items-center justify-center gap-3 border border-outline-variant/40 rounded-full py-3.5 bg-surface hover:bg-surface-container-low transition-all duration-200 hover:shadow-sm group cursor-pointer" do %>
          <svg class="w-5 h-5 flex-shrink-0" viewBox="0 0 24 24">
            <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
            <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
            <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l3.66-2.84z"/>
            <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
          </svg>
          <span class="font-label-caps text-label-caps text-on-surface uppercase tracking-widest text-sm group-hover:text-primary transition-colors">
            Google
          </span>
        <% end %>
      </div>

      <!-- Secondary Links -->
      <div class="flex flex-col items-center gap-4 border-t border-outline-variant/20 pt-8">
        <p class="font-body-sm text-body-sm text-outline">
          ¿Ya tienes cuenta?
          <%= link_to "Inicia sesión", new_session_path(resource_name),
                class: "text-primary font-medium hover:underline underline-offset-2" %>
        </p>
      </div>
    </div>

    <div class="mt-8 text-center opacity-40">
      <p class="font-label-caps text-[10px] tracking-[0.3em] uppercase text-outline">Exclusividad &amp; Precisión</p>
    </div>
  </main>
</div>
